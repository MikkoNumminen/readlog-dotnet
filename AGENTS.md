# Agent guidance

Agent (and human) guidance for this repository lives in **[CLAUDE.md](CLAUDE.md)** —
start there. It covers orientation, the prime directive, standing rules, commands,
layout, architecture, and the load-bearing invariants.

This file is a pointer so any agent convention (`AGENTS.md`, GitHub Copilot's
`.github/copilot-instructions.md`, etc.) resolves to the same single source of truth.
